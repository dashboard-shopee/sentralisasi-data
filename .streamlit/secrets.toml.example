# Contoh secrets untuk Streamlit Community Cloud.
# JANGAN commit file asli (secrets.toml) — isi ini di menu "Secrets" Streamlit Cloud.

# Koneksi database Supabase (Session pooler URI, password sudah di-URL-encode)
DATABASE_URL = "postgresql://postgres.xxxx:PASSWORD@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres"

# Password untuk membuka dashboard (biar tidak publik)
password = "rahasia-bos-2026"
